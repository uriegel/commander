{
  "name": "rust",
  "version": "1.0.0",
  "description": "",
  "main": "./index.node",
  "scripts": {
    "build-rust": "napi build --release && npm run copy",
    "copy": "cp index.node ../local_modules/rust && cp index.d.ts ../local_modules/rust cp package.json ../local_modules/rust"
  },  
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@napi-rs/cli": "^3.3.0"
  }
}